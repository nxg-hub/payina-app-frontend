import { Navbar, Sidebar } from '../_components';

const Inventory = () => {
  return (
    <div className="bg-primary">
      <Navbar />
      <Sidebar />
    </div>
  );
};

export default Inventory;
